import React from 'react'; // Importing React library to define components
import LoginForm from '../component/Login/LoginForm'; // Importing LoginForm component from its file

function Login() {
  return (
    <div><LoginForm /></div> // Rendering the LoginForm component within a div
  );
}

export default Login; // Exporting the Login component as a default export
